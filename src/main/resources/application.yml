server:
  port : 8085

parcom:
  services:
    classroom:
      host: pc-classroom
      port: 8080
spring:
  application.name: pc-notifier
  hateoas:
    use-hal-as-default-json-media-type: false
  data:
    rest:
      detection-strategy: annotated
